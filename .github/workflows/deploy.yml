name: Deploy to InfinityFree
on:
  push:
    branches:
      - main
jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: ftpupload.net
          username: epiz_12345678
          password: ${{ secrets.PBvdnjmd8qW3 }}
          server-dir: /htdocs/
